#include <vector>

using namespace std;

void quickSort(vector<int> &nums, int l, int r) {
    if (l + 1 >= r)
        return;
    int first = l, last = r - 1, key = nums[first];
    while (first < last) {

    }
}

